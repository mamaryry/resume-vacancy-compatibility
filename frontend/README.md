# Фронтенд системы анализа резюме

Фронтенд платформы анализа резюме на основе ИИ, построенный на React 18, Vite, TypeScript и Material-UI.

## Технологический стек

- **Фреймворк**: React 18 с TypeScript
- **Инструмент сборки**: Vite 5
- **UI-библиотека**: Material-UI (MUI) v6 с Emotion
- **Маршрутизация**: React Router v6
- **HTTP-клиент**: Axios
- **Тестирование**: Vitest + React Testing Library + Playwright (E2E)
- **Качество кода**: ESLint, Prettier

## Предварительные требования

- Node.js >= 18.0.0
- npm >= 9.0.0

## Установка

```bash
cd frontend
npm install
```

## Разработка

Запустите сервер разработки:

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:5173](http://localhost:5173)

## Доступные скрипты

### Разработка
- `npm run dev` - Запуск сервера разработки
- `npm run build` - Сборка для продакшена
- `npm run preview` - Локальный превью продакшен-сборки

### Юнит-тесты
- `npm run test` - Запуск юнит-тестов
- `npm run test:ui` - Запуск тестов с UI
- `npm run test:coverage` - Запуск тестов с отчётом о покрытии

### E2E-тесты
- `npm run test:e2e` - Запуск end-to-end тестов с Playwright
- `npm run test:e2e:ui` - Запуск E2E тестов в режиме UI (интерактивно)
- `npm run test:e2e:debug` - Отладка E2E тестов
- `npm run test:e2e:install` - Установка браузеров Playwright

### Качество кода
- `npm run lint` - Запуск ESLint
- `npm run format` - Форматирование кода с Prettier

## Структура проекта

```
frontend/
├── src/
│   ├── api/           # API-клиент и эндпоинты
│   ├── components/    # React-компоненты
│   ├── hooks/         # Пользовательские React hooks
│   ├── types/         # Определения типов TypeScript
│   ├── utils/         # Вспомогательные функции
│   ├── tests/         # Настройка тестов и утилиты
│   ├── App.tsx        # Корневой компонент приложения
│   ├── main.tsx       # Точка входа в приложение
│   └── index.css      # Глобальные стили
├── e2e/               # End-to-end тесты с Playwright
│   ├── fixtures/      # Фикстуры тестовых данных
│   └── README.md      # Документация по E2E тестированию
├── public/            # Статические ресурсы
├── index.html         # HTML-шаблон
├── package.json       # Зависимости и скрипты
├── playwright.config.ts # Конфигурация E2E тестов Playwright
├── vite.config.ts     # Конфигурация Vite
├── tsconfig.json      # Конфигурация TypeScript
└── .eslintrc.cjs      # Конфигурация ESLint
```

## Конфигурация

### Переменные окружения

Создайте файл `.env` в директории frontend (см. `.env.example`):

```bash
# URL API бэкенда
VITE_API_URL=http://localhost:8000

# Название приложения
VITE_APP_TITLE=Resume Analysis Platform
```

### API-прокси

Сервер разработки настроен для проксирования API-запросов к бэкенду:

- `/api/*` → `http://localhost:8000/api/*`
- `/health` → `http://localhost:8000/health`

## Возможности

- 📄 Загрузка резюме с поддержкой перетаскивания
- 🔍 Отображение результатов анализа в реальном времени
- 🎨 Компоненты Material-UI с кастомной темой
- 📊 Визуализация сопоставления вакансий с цветовой кодировкой навыков
- 🌐 Адаптивный дизайн для мобильных устройств и десктопа
- ♿ Поддержка доступности (WCAG 2.1 AA)

## Тестирование

### Юнит-тесты

Запустите тесты в режиме отслеживания:

```bash
npm run test
```

Запустите тесты с покрытием:

```bash
npm run test:coverage
```

### E2E-тесты

Предварительные требования:
1. API бэкенда запущен на `http://localhost:8000`
2. Сервер разработки фронтенда запущен на `http://localhost:5173`
3. Браузеры Playwright установлены (выполните `npm run test:e2e:install`)

Запустите все E2E тесты:

```bash
npm run test:e2e
```

Запустите E2E тесты в интерактивном UI режиме:

```bash
npm run test:e2e:ui
```

Отладка E2E тестов:

```bash
npm run test:e2e:debug
```

Подробную документацию по E2E тестированию см. в [e2e/README.md](e2e/README.md).

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут находиться в директории `dist/`.

## Стиль кода

В этом проекте используются:
- **ESLint** для линтинга
- **Prettier** для форматирования кода
- **Строгий режим TypeScript** для типобезопасности

## Лицензия

MIT
